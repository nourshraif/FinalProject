# Example environment configuration for FinalProject
#
# 1. Copy this file to `.env` in the project root:
#    cp env.example .env
#
# 2. Replace placeholder values with your actual credentials
#
# IMPORTANT: Do NOT commit your real `.env` file to git.
#            The .env file is already in .gitignore

# ============================================================================
# Database Configuration
# ============================================================================
# Postgres runs in Docker; app runs locally → use localhost and host port 5433
DB_HOST=localhost
DB_NAME=jobs_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_PORT=5433

# ============================================================================
# Hugging Face API Configuration
# ============================================================================
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here
HF_MODEL=openai/gpt-oss-120b:groq

