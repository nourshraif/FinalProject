"""
Database models.

At the moment the project uses raw SQL (see migration notes and README).
This module is provided as a placeholder so that ORM models (e.g. SQLAlchemy)
can be added later without changing the public package structure.
"""

